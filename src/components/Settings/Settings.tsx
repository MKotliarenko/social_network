import React from 'react';
import s from './Settings.module.css';

type SettingsType ={

}

export function Settings(props:SettingsType) {
    return (<div>
            Settings
        </div>
    );
}